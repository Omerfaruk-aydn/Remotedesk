# Shared Security Policies

- Remote control defaults to disabled and requires explicit host approval.
- Clipboard, file transfer and audio default to disabled.
- Pairing codes and refresh tokens are stored only as hashes.
- Input content is not logged; only event counts and coarse event types may be logged.
- Host visible session banner is mandatory and must not be hideable.
