# Viewer Windows App

WPF/MVVM viewer skeleton with a remote screen surface, session metrics toolbar and remote input service that refuses to send input until the host has explicitly approved remote control.
