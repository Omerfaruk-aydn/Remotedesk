# Host Windows App

WPF/MVVM host skeleton. Native `Windows.Graphics.Capture`, WebRTC video track and `SendInput` integrations are represented by testable interfaces plus safe mock implementations. Remote input is guarded by approved session id and explicit remote-control permission.
