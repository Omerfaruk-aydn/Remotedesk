# Admin Dashboard

Next.js App Router dashboard skeleton for administrators. It uses TypeScript strict mode, Tailwind CSS, Zod schemas and a typed API client.

```powershell
npm install
npm run dev
```
