# SecureRemoteDesk Desktop

Unified Windows desktop application. One executable contains both workflows:

- Host mode: share this computer with explicit approval.
- Viewer mode: request access to another computer by pairing code.
- Security logs: visible safety and session events.

Installer output:

```text
artifacts/installers/SecureRemoteDesk-Setup.exe
```
